# Configuración complementaria para el listener MQTT seguro.
# Se aplica después del .env local mediante dos opciones --env-file.
MOSQUITTO_CONFIG_FILE=/mosquitto/config/mosquitto-tls.conf
RMM_MQTT_HOST_PORT=8883
RMM_MQTT_PORT=8883
RMM_MQTT_TLS=true

