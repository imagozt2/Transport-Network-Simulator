package com.transport.simulator.enums;

public enum TicketOperationType {
    ISSUED,
    RECHARGED,
    ENTRY_ACCEPTED,
    EXIT_ACCEPTED,
    BLOCKED,
    UNBLOCKED,
    CANCELLED,
    SUPPORT_LINKED,
    QR_REVOKED
}
